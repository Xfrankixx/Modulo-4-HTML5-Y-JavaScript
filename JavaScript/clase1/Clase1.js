

let var= "Hello class";//Valor String normal

let name-"Frank";

val= val+' '+name;//concatenar cob espacio enmedio

val +=name;//sumara despues de nombre

val= name.length

val= concat(' ',val)//otra forma de concatenar, se concatena el especio y Valor

vale=name.toUpperCase(); //Cambia todo a mayuscula

val=name.toLowerCase();//todo a minuscula

val= name[1]//va a agarrar el primer valor del String, si es "Frank", seria la f

val=name.indexOf('')//buscara  el numero en el que el valor entre parentesis se encuentra, si busco "a" dentro de Frank, seria el numero 3, si es repetidos, toma el primer Valor

val=name.lastindexof('')//buscara el valor ingresado que se encuentre al final de la palabra

val= name.charAt('3')// traeme el caracter que esta en el indice 3

val=name.charAt(name.length-1)//para obtener el ultimo indice

val= name.substring(0,2); //de donde a donde quiero saber los indices, desde el 0 al 2 todas las letras

val=name.slice(0,3);//para decidir que parte de nuestro string tomaremos en cuenta

val=name.slice(-2)//desde el final-2 , imprimira todo osea si es ´Frank´, imprimira nk

list='lemon, pepper, pizza'

val= list.split(' ')//separa lo de la lista con espacios, y ordena con numeros

list= list.replace('lemon', 'water') //remplaze la parte que dice lemon por water

cal= 'David Grohl'

val=val.includes ('Omar')//se pregunta si esta dentro de val, lo que esta en el parentesis, de no ser asi, saldre false, o de ser asi true



console.log(val)
